"""audit_kit — a framework for auditing sentence/translation datasets.

See README.md for the full lifecycle. The public surface is the CLI
(`python -m audit_kit`); modules below are reusable building blocks.
"""
